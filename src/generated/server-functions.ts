// SPDX-License-Identifier: MIT
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated Client Functions
 *
 * Fetch-based CRUD operations that call the Hono REST API.
 * No server-side imports — safe for browser bundles.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  CompanyType,
  CompanyCreateInput,
  CompanyUpdateInput,
  DepartmentType,
  DepartmentCreateInput,
  DepartmentUpdateInput,
  AgentType,
  AgentCreateInput,
  AgentUpdateInput,
  TaskType,
  TaskCreateInput,
  TaskUpdateInput,
  AgentMessageType,
  AgentMessageCreateInput,
  AgentMessageUpdateInput,
  AgentMemoryType,
  AgentMemoryCreateInput,
  AgentMemoryUpdateInput,
  BrainMemoryType,
  BrainMemoryCreateInput,
  BrainMemoryUpdateInput,
  WorkflowType,
  WorkflowCreateInput,
  WorkflowUpdateInput,
  ActivityType,
  ActivityCreateInput,
  ActivityUpdateInput,
} from './types'

/** Get the API base URL */
function getApiBase(): string {
  if (typeof window !== 'undefined') {
    return window.location.origin
  }
  return process.env.API_URL || 'http://localhost:3001'
}

// ============================================================================
// User Client Functions
// ============================================================================

/**
 * List all User records
 */
export async function getUserList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/users${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list User')
  }
  const json = await response.json()
  return (json.items || []) as UserType[]
}

/**
 * Get a single User by ID
 */
export async function getUserById(args: { data: { id: string; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'User not found')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Create a new User
 */
export async function createUser(args: { data: { input: UserCreateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Update an existing User
 */
export async function updateUser(args: { data: { id: string; input: UserUpdateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Delete a User
 */
export async function deleteUser(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete User')
  }
  return { success: true }
}

// ============================================================================
// Company Client Functions
// ============================================================================

/**
 * List all Company records
 */
export async function getCompanyList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<CompanyType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/companies${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Company')
  }
  const json = await response.json()
  return (json.items || []) as CompanyType[]
}

/**
 * Get a single Company by ID
 */
export async function getCompanyById(args: { data: { id: string; userId?: string } }): Promise<CompanyType> {
  const url = `${getApiBase()}/api/companies/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Company not found')
  }
  const json = await response.json()
  return json.data as CompanyType
}

/**
 * Create a new Company
 */
export async function createCompany(args: { data: { input: CompanyCreateInput; userId?: string } }): Promise<CompanyType> {
  const url = `${getApiBase()}/api/companies`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Company')
  }
  const json = await response.json()
  return json.data as CompanyType
}

/**
 * Update an existing Company
 */
export async function updateCompany(args: { data: { id: string; input: CompanyUpdateInput; userId?: string } }): Promise<CompanyType> {
  const url = `${getApiBase()}/api/companies/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Company')
  }
  const json = await response.json()
  return json.data as CompanyType
}

/**
 * Delete a Company
 */
export async function deleteCompany(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/companies/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Company')
  }
  return { success: true }
}

// ============================================================================
// Department Client Functions
// ============================================================================

/**
 * List all Department records
 */
export async function getDepartmentList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<DepartmentType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/departments${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Department')
  }
  const json = await response.json()
  return (json.items || []) as DepartmentType[]
}

/**
 * Get a single Department by ID
 */
export async function getDepartmentById(args: { data: { id: string; userId?: string } }): Promise<DepartmentType> {
  const url = `${getApiBase()}/api/departments/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Department not found')
  }
  const json = await response.json()
  return json.data as DepartmentType
}

/**
 * Create a new Department
 */
export async function createDepartment(args: { data: { input: DepartmentCreateInput; userId?: string } }): Promise<DepartmentType> {
  const url = `${getApiBase()}/api/departments`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Department')
  }
  const json = await response.json()
  return json.data as DepartmentType
}

/**
 * Update an existing Department
 */
export async function updateDepartment(args: { data: { id: string; input: DepartmentUpdateInput; userId?: string } }): Promise<DepartmentType> {
  const url = `${getApiBase()}/api/departments/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Department')
  }
  const json = await response.json()
  return json.data as DepartmentType
}

/**
 * Delete a Department
 */
export async function deleteDepartment(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/departments/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Department')
  }
  return { success: true }
}

// ============================================================================
// Agent Client Functions
// ============================================================================

/**
 * List all Agent records
 */
export async function getAgentList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<AgentType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/agents${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Agent')
  }
  const json = await response.json()
  return (json.items || []) as AgentType[]
}

/**
 * Get a single Agent by ID
 */
export async function getAgentById(args: { data: { id: string; userId?: string } }): Promise<AgentType> {
  const url = `${getApiBase()}/api/agents/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Agent not found')
  }
  const json = await response.json()
  return json.data as AgentType
}

/**
 * Create a new Agent
 */
export async function createAgent(args: { data: { input: AgentCreateInput; userId?: string } }): Promise<AgentType> {
  const url = `${getApiBase()}/api/agents`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Agent')
  }
  const json = await response.json()
  return json.data as AgentType
}

/**
 * Update an existing Agent
 */
export async function updateAgent(args: { data: { id: string; input: AgentUpdateInput; userId?: string } }): Promise<AgentType> {
  const url = `${getApiBase()}/api/agents/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Agent')
  }
  const json = await response.json()
  return json.data as AgentType
}

/**
 * Delete a Agent
 */
export async function deleteAgent(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/agents/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Agent')
  }
  return { success: true }
}

// ============================================================================
// Task Client Functions
// ============================================================================

/**
 * List all Task records
 */
export async function getTaskList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<TaskType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/tasks${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Task')
  }
  const json = await response.json()
  return (json.items || []) as TaskType[]
}

/**
 * Get a single Task by ID
 */
export async function getTaskById(args: { data: { id: string; userId?: string } }): Promise<TaskType> {
  const url = `${getApiBase()}/api/tasks/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Task not found')
  }
  const json = await response.json()
  return json.data as TaskType
}

/**
 * Create a new Task
 */
export async function createTask(args: { data: { input: TaskCreateInput; userId?: string } }): Promise<TaskType> {
  const url = `${getApiBase()}/api/tasks`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Task')
  }
  const json = await response.json()
  return json.data as TaskType
}

/**
 * Update an existing Task
 */
export async function updateTask(args: { data: { id: string; input: TaskUpdateInput; userId?: string } }): Promise<TaskType> {
  const url = `${getApiBase()}/api/tasks/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Task')
  }
  const json = await response.json()
  return json.data as TaskType
}

/**
 * Delete a Task
 */
export async function deleteTask(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/tasks/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Task')
  }
  return { success: true }
}

// ============================================================================
// AgentMessage Client Functions
// ============================================================================

/**
 * List all AgentMessage records
 */
export async function getAgentMessageList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<AgentMessageType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/agent-messages${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list AgentMessage')
  }
  const json = await response.json()
  return (json.items || []) as AgentMessageType[]
}

/**
 * Get a single AgentMessage by ID
 */
export async function getAgentMessageById(args: { data: { id: string; userId?: string } }): Promise<AgentMessageType> {
  const url = `${getApiBase()}/api/agent-messages/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'AgentMessage not found')
  }
  const json = await response.json()
  return json.data as AgentMessageType
}

/**
 * Create a new AgentMessage
 */
export async function createAgentMessage(args: { data: { input: AgentMessageCreateInput; userId?: string } }): Promise<AgentMessageType> {
  const url = `${getApiBase()}/api/agent-messages`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create AgentMessage')
  }
  const json = await response.json()
  return json.data as AgentMessageType
}

/**
 * Update an existing AgentMessage
 */
export async function updateAgentMessage(args: { data: { id: string; input: AgentMessageUpdateInput; userId?: string } }): Promise<AgentMessageType> {
  const url = `${getApiBase()}/api/agent-messages/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update AgentMessage')
  }
  const json = await response.json()
  return json.data as AgentMessageType
}

/**
 * Delete a AgentMessage
 */
export async function deleteAgentMessage(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/agent-messages/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete AgentMessage')
  }
  return { success: true }
}

// ============================================================================
// AgentMemory Client Functions
// ============================================================================

/**
 * List all AgentMemory records
 */
export async function getAgentMemoryList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<AgentMemoryType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/agent-memories${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list AgentMemory')
  }
  const json = await response.json()
  return (json.items || []) as AgentMemoryType[]
}

/**
 * Get a single AgentMemory by ID
 */
export async function getAgentMemoryById(args: { data: { id: string; userId?: string } }): Promise<AgentMemoryType> {
  const url = `${getApiBase()}/api/agent-memories/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'AgentMemory not found')
  }
  const json = await response.json()
  return json.data as AgentMemoryType
}

/**
 * Create a new AgentMemory
 */
export async function createAgentMemory(args: { data: { input: AgentMemoryCreateInput; userId?: string } }): Promise<AgentMemoryType> {
  const url = `${getApiBase()}/api/agent-memories`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create AgentMemory')
  }
  const json = await response.json()
  return json.data as AgentMemoryType
}

/**
 * Update an existing AgentMemory
 */
export async function updateAgentMemory(args: { data: { id: string; input: AgentMemoryUpdateInput; userId?: string } }): Promise<AgentMemoryType> {
  const url = `${getApiBase()}/api/agent-memories/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update AgentMemory')
  }
  const json = await response.json()
  return json.data as AgentMemoryType
}

/**
 * Delete a AgentMemory
 */
export async function deleteAgentMemory(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/agent-memories/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete AgentMemory')
  }
  return { success: true }
}

// ============================================================================
// BrainMemory Client Functions
// ============================================================================

/**
 * List all BrainMemory records
 */
export async function getBrainMemoryList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<BrainMemoryType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/brain-memories${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list BrainMemory')
  }
  const json = await response.json()
  return (json.items || []) as BrainMemoryType[]
}

/**
 * Get a single BrainMemory by ID
 */
export async function getBrainMemoryById(args: { data: { id: string; userId?: string } }): Promise<BrainMemoryType> {
  const url = `${getApiBase()}/api/brain-memories/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'BrainMemory not found')
  }
  const json = await response.json()
  return json.data as BrainMemoryType
}

/**
 * Create a new BrainMemory
 */
export async function createBrainMemory(args: { data: { input: BrainMemoryCreateInput; userId?: string } }): Promise<BrainMemoryType> {
  const url = `${getApiBase()}/api/brain-memories`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create BrainMemory')
  }
  const json = await response.json()
  return json.data as BrainMemoryType
}

/**
 * Update an existing BrainMemory
 */
export async function updateBrainMemory(args: { data: { id: string; input: BrainMemoryUpdateInput; userId?: string } }): Promise<BrainMemoryType> {
  const url = `${getApiBase()}/api/brain-memories/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update BrainMemory')
  }
  const json = await response.json()
  return json.data as BrainMemoryType
}

/**
 * Delete a BrainMemory
 */
export async function deleteBrainMemory(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/brain-memories/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete BrainMemory')
  }
  return { success: true }
}

// ============================================================================
// Workflow Client Functions
// ============================================================================

/**
 * List all Workflow records
 */
export async function getWorkflowList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<WorkflowType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/workflows${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Workflow')
  }
  const json = await response.json()
  return (json.items || []) as WorkflowType[]
}

/**
 * Get a single Workflow by ID
 */
export async function getWorkflowById(args: { data: { id: string; userId?: string } }): Promise<WorkflowType> {
  const url = `${getApiBase()}/api/workflows/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Workflow not found')
  }
  const json = await response.json()
  return json.data as WorkflowType
}

/**
 * Create a new Workflow
 */
export async function createWorkflow(args: { data: { input: WorkflowCreateInput; userId?: string } }): Promise<WorkflowType> {
  const url = `${getApiBase()}/api/workflows`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Workflow')
  }
  const json = await response.json()
  return json.data as WorkflowType
}

/**
 * Update an existing Workflow
 */
export async function updateWorkflow(args: { data: { id: string; input: WorkflowUpdateInput; userId?: string } }): Promise<WorkflowType> {
  const url = `${getApiBase()}/api/workflows/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Workflow')
  }
  const json = await response.json()
  return json.data as WorkflowType
}

/**
 * Delete a Workflow
 */
export async function deleteWorkflow(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/workflows/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Workflow')
  }
  return { success: true }
}

// ============================================================================
// Activity Client Functions
// ============================================================================

/**
 * List all Activity records
 */
export async function getActivityList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ActivityType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/activities${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Activity')
  }
  const json = await response.json()
  return (json.items || []) as ActivityType[]
}

/**
 * Get a single Activity by ID
 */
export async function getActivityById(args: { data: { id: string; userId?: string } }): Promise<ActivityType> {
  const url = `${getApiBase()}/api/activities/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Activity not found')
  }
  const json = await response.json()
  return json.data as ActivityType
}

/**
 * Create a new Activity
 */
export async function createActivity(args: { data: { input: ActivityCreateInput; userId?: string } }): Promise<ActivityType> {
  const url = `${getApiBase()}/api/activities`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Activity')
  }
  const json = await response.json()
  return json.data as ActivityType
}

/**
 * Update an existing Activity
 */
export async function updateActivity(args: { data: { id: string; input: ActivityUpdateInput; userId?: string } }): Promise<ActivityType> {
  const url = `${getApiBase()}/api/activities/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Activity')
  }
  const json = await response.json()
  return json.data as ActivityType
}

/**
 * Delete a Activity
 */
export async function deleteActivity(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/activities/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Activity')
  }
  return { success: true }
}
